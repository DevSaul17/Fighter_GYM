import 'package:flutter/material.dart';

class RegistrarHorarioScreen extends StatelessWidget {
  const RegistrarHorarioScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Registrar Horario')),
      body: const Center(child: Text('Registrar Horario')),
    );
  }
}
